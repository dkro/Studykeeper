<h2 class="management--single__heading">Template bearbeiten</h2>

<button class="btn btn-danger management--single__deleteButton" {{bind-attr disabled=isDeleteLoading}} {{action 'deleteTemplate'}}>
    {{#if isDeleteLoading}}
        {{loading-status additionalClassNames="loading--delete"}}
    {{else}}
        Template löschen
    {{/if}}
</button>

{{#if statusMessage}}
    {{status-message message=statusMessage.message isSuccess=statusMessage.isSuccess}}
{{/if}}

{{template-config isTemplateCreation=false title=title fields=fields isSaveLoading=isUpdateLoading
                  createUpdateDataWasValid=updateDataWasValid relatedStudies=relatedStudies
                  clickOnStudy="goToStudy"save="updateTemplate" cancel="cancelView"}}
